name: Link Checker

on:
  # 1. Trigger on Pull Requests to catch errors before merging
  pull_request:
    branches: [main]

  # 2. Trigger on every Push to the main branch
  push:
    branches: [main]

  # 3. Keep the Sunday schedule for long-term monitoring
  schedule:
    - cron: '0 0 * * 0' 

  # 4. Keep the manual button just in case
  workflow_dispatch: 

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Link Checker
        uses: lycheeverse/lychee-action@v2
        with:
          args: --verbose --no-progress --retry-wait 5 --max-retries 3 README.md # This tells the checker to wait 5 seconds and try up to 3 times per link
          fail: true # This makes the "Action" tab show a red X if a link is broken
